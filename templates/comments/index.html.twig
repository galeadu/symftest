{% extends 'base.html.twig' %}

{% block title %}Poznámky{% endblock %}

{% block body %}

<div class="container">
    <div class="row">
        <div class="col"> 
            <h1>
                Poznámky
             </h1> 
        </div>
    </div>
    <div class="row"> 
        <div class="col">
            <p>
                <a href="/comments/form">Nová poznámka</a>
            </p> 
        </div>
    </div>
    {% if comments %}
        {% for item in comments %}
            <div class="row">
                <div class="col-4 col-sm-2">{{ item.dateinsert|date('Y-m-d H:i:s') }}</div>
                <div class="col">{{ item.shortdescription }}</div>
                <div class="col-1"><a href="/comments/detail/{{ item.id }}">Detail</a></div>
                <div class="col-1"><a href="/comments/delete/{{ item.id }}">Smazat</a></div>
            </div>
        {% endfor %}
    {% endif %}
    </div>

{% endblock %}
